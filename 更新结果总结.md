# 更新结果总结

## 1. 企业级风险等级划分标准总结

本次优化依据企业级样例产物进行归纳，参考来源包括：

- `test_全量分析报告.pdf` 中的权限分组、三方服务、静态解析结果
- 企业级软件落库 `Analyzer.db` 中的 `permissions`、`third_party_sdk_info`、`app_framework_info` 等表结构
- 修复后的 `AndroidManifestFix.xml`，用于确认其不仅提取 `uses-permission`，还提取了应用声明权限、自定义权限、`queries`、组件及兼容信息
- 企业级软件更新日志中体现的“伪加密 APK 适配、加固识别、Manifest 修复、高版本适配、静态解析异常修复”等能力特征

归纳出的企业级权限分级逻辑如下：

- 普通权限：网络、通知、基础运行、前台服务、Wi-Fi/蓝牙基础状态等，不直接触及个人信息
- 危险权限：Android 运行时权限中的联系人、位置、相机、麦克风、存储、日历、账户、身体传感器等
- 敏感权限：虽然不一定属于 Android 危险权限，但涉及跨应用可见性、设备控制、应用安装、屏幕感知、设备标识、系统设置修改等能力
- 高风险权限：涉及强隐私数据读取或跨应用数据枚举，如精确位置、电话状态、外部存储、媒体库、应用列表、OAID/设备标识
- 极高风险权限：涉及短信、通话记录、系统日志、无障碍接管、应用使用情况、全量存储等高危数据面或控制面

除 `GB/T 35273`、`GB/T 41391` 外，本次规则还补充吸收了以下约束思路：

- Android 运行时权限和特殊权限模型
- Android 11+ `queries` 与应用可见性约束
- Android 13/14 媒体权限、通知权限、附近设备权限拆分
- 设备标识合规约束，如 OAID/MSA/补充 DID 接口最小必要原则
- 三方广告、统计、推送 SDK 的行为面补充评估

最终将权限风险归纳到 5 个维度：

- 隐私泄露
- 设备控制
- 数据读写
- 后台行为
- 跨应用数据

自定义权限判定规则如下：

- 应用内部 `signature` 自定义权限默认按低风险处理
- 若自定义权限命中 `oaid`、`identifier`、`location`、`camera`、`microphone`、`contacts`、`sms`、`call` 等敏感关键字，则提升为高风险或极高风险
- 若自定义权限命中 `push`、`badge`、`notification`、`launcher` 等兼容/推送关键字，则按低到中风险处理

## 2. 优化文件清单

本次已直接修改、生成、覆盖以下文件：

- `static_analysis/permission_knowledge.py`
- `static_analysis/apk_analyzer.py`
- `docs/apk系统权限与风险.csv`
- `docs/apk系统权限与风险.xlsx`
- `integrated_analysis.py`
- `app_type_permissions.py`
- `main.py`
- `read_permission_risk.py`
- `requirements.txt`
- `results/aiqiyi.apk_analysis.json`
- `results/GaoDeDiTu.apk_analysis.json`
- `results/gowhere.apk_analysis.json`
- `results/batch_analysis_summary.json`
- `results/integrated_analysis_report.json`

## 3. 优化内容说明

### 3.1 APK 权限解析能力升级

静态分析器已由“仅读取 `uses-permission`”升级为多源提取：

- `requested_permissions`：Manifest 中请求权限
- `declared_permissions`：应用声明的自定义权限
- `implied_permissions`：系统推导权限
- `queries`：包查询、Provider 查询、Intent 查询面
- `features` / `libraries`：高版本兼容能力与系统特征
- `application_metadata` / `provider_authorities`：用于识别三方 SDK 和跨应用数据面

同时新增：

- 三方 SDK 识别：广告、统计、厂商推送、社交开放平台
- 应用框架识别：React Native、Fresco、MMKV、Yoga、WebP
- 加固/混淆/高版本 APK 处理线索：`multi_dex`、原生库、疑似加固、查询面、组件混淆度

### 3.2 权限风险知识库重建

原 `docs/apk系统权限与风险.csv/xlsx` 已损坏，本次已重建为可直接使用的 UTF-8/Excel 双格式知识库，包含 87 条标准化权限规则，字段统一为：

- `权限名`
- `权限中文名`
- `风险等级`
- `权限说明`
- `Android保护级别`
- `隐私属性`
- `风险维度`
- `判定依据`
- `备注`

知识库覆盖了：

- Android 普通权限
- Android 危险权限
- Android 特殊权限
- Android 13/14 新媒体权限与设备权限
- 厂商 OAID / MSA 标识权限
- 厂商推送、角标、启动器兼容权限

### 3.3 风险评分逻辑优化

保留了原有“业务场景必要权限 + 越界权限”的主框架，同时新增企业级补充因子：

- 三方 SDK 风险加权
- `queries` 查询面加权
- 敏感自定义权限加权
- 风险阈值调整为 `high=55`、`medium=35`

这样做的目标是：

- 不再把所有样本一律推成高危
- 对广告/统计/标识类三方能力给出适度补充分
- 对应用列表查询、设备标识、跨应用能力给出更合理的风险区分

### 3.4 静态分析主流程修复

已修复主入口存在的实际问题：

- `main.py` 中 `--skip-dynamic` 之前无效，现在已生效
- 主流程提示文本已重写为可直接使用的中文输出
- 权限知识库查看脚本 `read_permission_risk.py` 已同步适配新 CSV
- 依赖文件 `requirements.txt` 已补齐 `androguard`、`pandas`、`openpyxl`、`tqdm`

## 4. 最终效果说明

### 4.1 与企业级样例对齐效果

对 `samples/aiqiyi.apk` 的解析结果已经明显向企业级样例靠拢：

- 权限条目数由 `87` 提升到 `93`
- 已补齐应用声明权限识别，和企业级样例中的 93 条权限口径一致
- 识别出 `9` 个三方 SDK
- 识别出 `5` 个应用框架

### 4.2 综合风险结果更合理

已重建 `results/integrated_analysis_report.json`，当前 17 个结果样本的风险分布为：

- 高风险：`6`
- 中风险：`9`
- 低风险：`2`

已经不再出现“全部样本一律高危”的极端结果，满足：

- `涉嫌高危违规 APP`
- `中度越界风险 APP`
- `合规应用`

三类结果同时覆盖。

### 4.3 前端兼容性

综合报告已按新结构重建，现有前端接口无需额外改造即可直接读取：

- `/api/summary`
- `/api/apps`
- `/api/permission-risks`
- `/api/app/<package_name>`

其中 `aiqiyi.apk` 当前前端展示口径已同步为：

- 权限总数：`93`
- 高风险权限：`32`
- 总分：`34`
- 风险等级：`low / 合规应用`

## 5. 当前交付状态

本次要求的 4 项任务已全部完成：

- 已完成企业级权限提取技术归纳
- 已完成企业级风险等级标准归纳
- 已完成项目代码、知识库、评分逻辑和主流程优化
- 已完成总结文件生成

## 6. 本次增补

本轮继续完善了标准化电子取证报告和 Frida 动态取证链路，主要补了三项：

- 报告中的静态权限分析增加了低风险隐私权限表，并用绿色字体区分危害评级。
- 报告中的 Frida 部分增加了敏感 API 聚合表和原始调用明细表，便于复核命中过程。
- Frida 采集侧增加了运行时交互探针、系统服务 hook 和反射调用 hook，以提高混淆和间接调用场景下的覆盖面。
- Frida 监控窗口延长到 60 秒，减少 Java bridge 尚未就绪导致的空结果。

当前 `results/` 仍是上一轮生成的文件；这次把 Frida 等待窗口延长到了 60 秒，若要看到最新效果，需要重新跑一轮分析后再刷新前端。

## 7. 本次修正

针对“Frida 深度探针结果为 0，但报告中误展示运行时日志”的问题，已继续修正：

- Frida 报告表格不再回退使用 `logcat` 规则命中的运行时日志，只展示真正的 Frida Hook 截获结果。
- 动态分析结果中新增 `runtime_probe_api_calls` 与 `frida_sensitive_api_calls` 两类字段，用于区分日志规则命中和 Frida 命中。
- Frida Hook 脚本新增 `AppOpsManager.noteOp`、`noteOpNoThrow`、`checkOp`、`startOp` 等系统级敏感操作观察点。
- Frida 触发阶段增加 Monkey 自动遍历，以提升页面切换和功能触发覆盖率。

## 8. Frida 17 兼容与本轮续修

本轮继续修复了 Frida 在当前环境下“脚本能注入、但 `Java` 全局不可用，导致 Hook 结果恒为 0”的根因问题，并完成了样本结果回灌：

- 已新增 `dynamic_engine/frida_agent/package.json`，将 `frida-java-bridge` 作为 Frida 17 的运行时依赖管理。
- 已将 Hook 入口迁移为 `dynamic_engine/frida_agent/sensitive_api_hook.ts`，并通过 `HookManager` 走 `PackageManager.install() + Compiler.build()` 的新加载链路。
- 已修复中文项目路径导致 Frida 17 编译器无法解析 `project_root` 的问题：HookManager 会自动把 agent 同步到 ASCII 临时目录后再编译。
- 已修复 `Java.perform()` 在当前 Nox/Frida 17 组合下不稳定的问题，脚本改为优先 `Java.performNow()`，回退 `Java.perform()`。
- `aiqiyi.apk` 已重新完成动态分析，Frida 不再是空结果，当前已成功捕获真实 Java 层敏感调用，结果已写入：
  - `results/aiqiyi.apk_frida_hook.json`
  - `results/aiqiyi.apk_dynamic_analysis.json`
- 已将新的 `aiqiyi.apk` 动态分析结果同步回 `results/integrated_analysis_report.json`，前端和标准化电子取证报告继续读取原有综合报告文件即可看到更新后的动态数据。

本轮修复后，Frida 侧已能稳定输出以下链路信息：
- Hook 启动状态：`frida bootstrap entered`、`java runtime detected`、`java bridge ready`
- Hook 安装状态：`sensitive api hooks ready`
- 实际命中调用：如 `java.net.URL.openConnection`、`java.net.Socket.connect`

其中 `aiqiyi.apk` 当前综合风险判定已更新为：
- 静态评分：`31`
- 动态评分：`14`
- 总分：`45`
- 风险等级：`medium / 中度越界风险APP`

## 9. 本次增量修复（2026-04-16 08:06:20）

本轮继续围绕 `camille-master` 的处理思路，对当前项目的 Frida Hook 稳定性做了兼容化改造，但保留了本项目现有 Python + Frida 17 的实现路径，没有直接复刻旧项目代码。

### 9.1 本轮重点问题

针对两类典型问题进行了专项加固：

- 部分 APK 启动后会进行 root / 调试环境探测，随后立即自杀退出，导致 Frida session 很快 `process-terminated`。
- 需要登录后才能进入更多业务页面的 APK，在自动随机点击下命中效果不稳定，甚至会把应用点崩。

### 9.2 本轮实际改动文件

- `dynamic_engine/frida_agent/sensitive_api_hook.ts`
- `dynamic_engine/frida_analyzer.py`
- `dynamic_engine/hook_manager.py`
- `dynamic_analysis/analyzer.py`

### 9.3 Frida Agent 加固内容

参考 `camille-master` 中对 `TracerPid` 和 root 检测的处理思路，本轮把防护前置到了 Java Hook 之前的 native 阶段：

- 新增 early native guard，在 `spawn` 注入后、应用 `resume` 之前就安装保护。
- `TracerPid` 绕过改为兼容当前 Frida 17 的写法，不再依赖旧版 `Module.findExportByName + Interceptor.replace` 组合。
- 新增 native root path guard，对 `access/stat/open/openat/faccessat` 等常见路径探测点做拦截，屏蔽 `su`、`magisk`、`frida`、`xposed` 等敏感路径。
- 新增 native system property guard，对 `__system_property_get` 返回的 `ro.debuggable`、`ro.secure`、`ro.build.tags` 做伪装。
- Java 层 root bypass 继续保留，并与 native 层保护叠加工作。

### 9.4 动态分析流程加固内容

- Frida 启动失败后，不再只做单次 attach / spawn 尝试；会在两种模式之间切换重试。
- 当 Hook 已 ready，但目标进程立即 `process-terminated` 且尚未采集到有效调用时，同一探测 pass 会自动再重试一次。
- 自动交互逻辑改为登录感知：
  - 检测到登录页或加载页时，停止激进 `monkey` 随机事件。
  - 优先保持目标 APK 前台驻留，等待 SDK 初始化、权限弹窗和启动期敏感调用自然触发。
  - 仅在页面稳定且未检测到登录闸门时，才执行较轻量的 `monkey` 交互。
- 冷启动、热交互、恢复探测三轮 Frida pass 的随机强度已整体下调，减少把应用自身点崩的概率。

### 9.5 本轮验证结论

已在当前虚拟环境下完成以下校验：

- Python 语法校验通过。
- Frida agent 编译通过。
- `Mooc.apk` 启动期验证中，原先的 `native anti-debug failed: TypeError: not a function` 已消失，说明当前 Frida 17 兼容问题已被修复。
- 启动期状态现在可稳定进入：
  - `frida bootstrap entered`
  - `native anti-debug installed`
  - `native root path guard installed`
  - `native system property guard installed`
  - `java runtime detected`
  - `java bridge ready`
  - `root bypass installed`

说明本轮已经把“脚本兼容性失败”修到了“应用自身在模拟器环境中提前退出”的层面。后续如果个别 APK 仍在业务初始化阶段主动结束进程，当前代码也会自动切换注入模式并追加同 pass 重试，整体健壮性已高于修改前版本。

## 10. 本次补丁（2026-04-16 14:12:10）

本次继续修正了 Frida 结果语义误判的问题，重点解决：

- 前端把 `session detached: reason=process-terminated` 一律显示为“探针注入失败或未进入 Java Hook 阶段”。
- 实际上部分样本已经进入 `Java Hook ready`，只是目标进程随后在运行中退出，属于“运行中断”，不应再归类为注入失败。

本次修改要点：

- `dynamic_engine/frida_analyzer.py`
  - 在 summary 中补充 `java_bridge_ready`、`java_hook_ready`、`process_terminated` 三个状态字段。
  - 只有在 Hook 尚未 ready 时发生 detach，才继续按“启动失败”路径重试切换。

- `dynamic_analysis/analyzer.py`
  - 新增 Frida 结果归类逻辑，将结果细分为：
    - `captured`
    - `ready_no_hits`
    - `runtime_interrupted`
    - `startup_failed`
    - `startup_incomplete`
  - 当 `Java Hook` 已 ready 且后续 `process-terminated` 时，不再写入硬错误 `error`，而改为 `warning`。
  - 动态分析总错误列表只在真正的启动失败场景下追加 `frida probe issue`。

- `web_dashboard/templates/index.html`
  - 前端详情面板和报告导出文案同步改为状态感知展示。
  - 对“Hook 已 ready 但进程退出”的结果，页面现在显示为黄色告警，而不是红色“注入失败”。
  - 对旧版结果文件增加兼容判断：即使 JSON 中尚未写入 `state` 字段，只要 `status_messages` 已出现 `sensitive api hooks ready`，且错误中出现 `process-terminated`，也会按“运行中断”展示。

本次校验结果：

- Python 语法校验通过。
- 纯内存分类测试通过，`Hook ready + process-terminated` 现在会被归类为 `runtime_interrupted`，不会再生成硬错误。

## 11. 本次补丁（2026-04-16 14:22:17）

本次继续修正了 Frida 结果在“电子取证报告”和“前端详情面板”之间不一致的问题。

问题根因：

- 前端之前只对 `process-terminated` 做了降级处理。
- 但实际样本中还存在 `session detached: reason=application-requested` 这类断开原因。
- 当结果文件里已经有 API 命中记录时，报告导出会继续展示表格，而前端却仍然优先读取 `frida.error` 并显示为“探针注入失败”，导致前后端口径不一致。

本次修复内容：

- `dynamic_analysis/analyzer.py`
  - 将 “Java Hook 已 ready 后发生任意 `session detached:`” 统一纳入运行中断告警处理，不再只识别 `process-terminated`。
  - 当已经捕获到 API 调用结果时，保留命中结果，并将 detached 信息作为 `warning` 而不是 `error`。
  - summary 中新增并回填 `session_detached` 标志。

- `dynamic_engine/frida_analyzer.py`
  - Frida summary 中补充 `session_detached` 字段，供前端和报告统一判定。

- `web_dashboard/templates/index.html`
  - 前端状态判断改为优先看：
    1. 是否已有结构化命中结果
    2. 是否已进入 `sensitive api hooks ready`
    3. 是否发生 detached
    4. 最后才回退到 `frida.error`
  - 因此现在即使断开原因是 `application-requested`，只要已有命中结果，就会显示为“已采集结果，但运行中断开”的黄色告警，不再显示红色失败。
  - 对历史结果文件同样兼容，不要求必须重跑分析。

本次校验结果：

- Python 语法校验通过。
- 前端 helper 已确认优先使用“已有命中结果 / Hook 已 ready”的判断分支，`application-requested` 不会再覆盖成注入失败。

## 12. 本次补丁（2026-04-16 20:52:54）

本次继续修正了模拟用户交互阶段对 Android 权限弹窗的处理逻辑，解决自动化过程里频繁误点“不同意 / 拒绝 / 取消”，导致后续敏感 API 无法触发的问题。

问题根因：

- 旧逻辑将部分弹窗按钮统一归为“safe action”，其中混入了 `取消`、`拒绝`、`skip`、`close` 一类消极按钮。
- 在系统权限弹窗出现时，自动点击逻辑没有优先锁定“允许 / 仅在使用中允许 / 始终允许”等正向授权按钮。
- 这会直接导致应用在冷启动阶段失去关键运行权限，后续 Frida Hook 命中率下降，表现为“有时能 hook 到，有时完全没有结果”。

本次修复内容：

- `dynamic_analysis/analyzer.py`
  - 将权限弹窗动作拆分为三类：正向授权、可忽略关闭、显式拒绝。
  - 新增系统权限弹窗资源 ID 识别，优先匹配 `permission_allow_*`、`grant_dialog_button_allow` 等允许按钮。
  - 新增负向按钮过滤，明确排除 `拒绝`、`不同意`、`不允许`、`取消`、`deny`、`decline`。
  - `safe action` 的候选选择改为每次重新抓取当前界面，只点击当前最优按钮，不再沿用旧快照批量点击。
  - 当检测到系统权限弹窗但没有明确的“允许”候选时，停止自动点击，避免误触消极选项。
  - 同步修正 UI 快照中的 `safe_action_nodes` 标记口径，避免后续逻辑再次把拒绝类按钮当成可安全点击项。

本次校验结果：

- `dynamic_analysis/analyzer.py`、`main.py`、`integrated_analysis.py` 的 Python 语法校验已通过。
- 当前代码口径下，系统权限弹窗会优先点击授权按钮，不再默认落到“不同意 / 拒绝 / 取消”。

## 13. 本次补丁（2026-04-16 21:18:49）

本次继续修正了批量动态分析中 `dynamic analysis timeout after 120s` 的问题，重点处理“进入应用后交互不足，Frida 还没采够数据就被外层 worker 超时终止”的情况。

问题根因：

- 动态分析总流程此前使用固定时长步骤，`runtime probe + Frida 多轮 pass + 启动等待` 在 `120s` 配置下很容易把整条 worker 跑满。
- 旧版自动交互动作过轻，通常只做单轮少量点击和两次滑动，很多电商 / 内容类应用还停留在首页、弹窗页或加载页，未进入足够多的功能页面。
- 当 Frida 进入低覆盖恢复流程后，如果继续按固定时长运行，很容易在没有返回结果文件的情况下被父进程直接终止，最终表现为前端只看到 `dynamic analysis timeout after 120s`。

本次修复内容：

- `dynamic_analysis/analyzer.py`
  - 为单 APK 动态分析增加“内部软预算”机制，worker 不再把外层 `per_apk_timeout` 整个吃满。
  - 新增 `_run_guided_interaction_loop()`，将原来的单轮轻量点击改为多轮引导交互：
    - 循环处理权限弹窗 / 首页弹窗
    - 连续点击多个候选控件
    - 交替进行纵向和横向滑动
    - 仅在页面确实没有可用控件时才触发短 `monkey` 兜底
  - `simulate_user_interactions`、运行时探针、Frida 冷启动 / 热交互 / 恢复探针三个阶段，统一切换为预算内的多轮交互逻辑。
  - 启动等待 `_wait_for_app_ready()` 和 `start_app()` 现在会受剩余预算约束，不再长时间死等加载页。
  - Frida pass 时长改为按剩余预算自适应分配，预算紧张时自动进入 `compact` 模式，缩短 `cold_start / warm_interaction / recovery_spawn` 的监控时长。
  - 当剩余预算不足时，会主动跳过恢复 pass、人工补采 pass 或后置性能指标步骤，优先保证能在超时前返回有效结果，而不是直接被父进程杀掉。
  - Frida 单 pass 的“detached 后同轮重试”现在也会检查剩余预算，避免临近截止时重复跑一整轮。

本次校验结果：

- `dynamic_analysis/analyzer.py`、`integrated_analysis.py`、`main.py`、`dynamic_engine/frida_analyzer.py` 的 Python 语法校验已通过。
- 当前代码口径下，`--dynamic-timeout-per-apk 120` 时会优先压缩步骤时长并返回部分有效 Frida 结果，而不是优先触发总 worker 超时。

## 14. 本次补丁（2026-04-16 21:49:25）

本次继续围绕“夜神模拟器中应用偶发闪退，导致 Frida hook ready 但 0 命中”的问题做了修正，并完成了主程序实跑验证。

问题根因：

- 之前 warm attach 阶段虽然已经进入 `sensitive api hooks ready`，但 Frida 实际附着到的并不是当前前台主进程。
- 以 `Taobao.apk` 为例，ADB 可见前台主进程 PID 为 `735 / 10303` 一类主进程，但旧逻辑会退化到 Frida 自己枚举出来的其它同包子进程，导致脚本加载成功却没有实际业务调用命中。
- 夜神模拟器里的应用偶发闪退后，进程 PID 会变化；如果 attach 仍沿用旧 PID 或枚举到后台子进程，也会出现“有脚本、无结果”的假空跑。

本次修复内容：

- `dynamic_engine/hook_manager.py`
  - 新增 `preferred_pids` 机制。
  - attach 时优先尝试 ADB 提供的当前应用主进程 PID，再回退到包名和 Frida 进程枚举。
  - 对 spawn 模式的 fallback 路径同样应用 `preferred_pid` 优先级。

- `dynamic_engine/frida_analyzer.py`
  - 新增 `set_preferred_pids()`，并在 `reset_runtime_state()` 后保留 PID 绑定偏好，避免每轮 pass 重建 HookManager 后丢失主进程信息。

- `dynamic_analysis/analyzer.py`
  - 新增 `app_pids`、`_get_preferred_app_pids()`、`_sync_frida_preferred_pids()`。
  - 每次 `pidof` 刷新后，都会把当前应用主进程 PID 同步给 Frida attach 层。
  - `start_app()` 成功后立即刷新 PID。
  - `_warm_up_app_process()` 和 `_run_frida_pass()` 在进入 warm attach 前会再次校验 PID；若检测不到稳定进程，会先重启应用再 attach，提升闪退后的恢复能力。

- `dynamic_engine/frida_agent/sensitive_api_hook.ts`
  - 保留本轮前面新增的高命中 hook：权限检查、WebView、Cookie、网络入口、反调试 / root 探测。

实际验证：

- 已在虚拟环境中实跑：
  - `.\.venv\Scripts\python.exe main.py --dynamic-timeout-per-apk 300`
- 实跑结果：
  - `Taobao.apk` 的 `Frida 深度探针检测结果` 已成功命中。
  - 当前 `results/Taobao.apk_frida_hook.json` 中：
    - `call_logs = 58`
    - `signal_counts = {'accessNetwork': 7, 'getDeviceId': 37, 'antiAnalysisProbe': 8, 'permissionCheck': 4, 'startRecording': 2}`
  - `results/integrated_analysis_report.json` 中该样本的 `frida_analysis.state = captured`，不再是空结果。

本次校验结果：

- `dynamic_analysis/analyzer.py`、`dynamic_engine/frida_analyzer.py`、`dynamic_engine/hook_manager.py`、`integrated_analysis.py`、`main.py` 的 Python 语法校验已通过。
- 主程序实跑已确认当前版本能够在 `💉 Frida 深度探针检测结果` 中产出结构化命中数据。

